// Test setup file for vitest
// This file is run before all tests

import { beforeEach } from 'vitest';

// Reset any global state before each test
beforeEach(() => {
    // Clear any mocks or global state if needed
});
