max_width = 120
reorder_imports = true
imports_granularity = "Module"
group_imports = "StdExternalCrate"
# Symlink stored as text on Windows — not valid Rust, skip formatting
ignore = ["crates/ironrdp-testsuite-core/tests/server"]
